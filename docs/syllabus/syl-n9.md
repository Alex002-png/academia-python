# SYL-N9 — Syllabus del Nivel 9 · Sistemas Distribuidos

## Control del documento (conforme a DOC-00 §15.4)

| Campo | Valor |
|---|---|
| **Document Key** | SYL-N9 · Tier T2 |
| **Versión / Estado** | **0.7.0** · Paso 5 del flujo institucional completo (compuerta) — los 4 módulos de N9 (M1-M4, 66 días, 138 ejercicios), el Capstone ET9 (`n9et9`, 6 hitos), y la compuerta (instrumentos RM-02/03/05/06 asignados por DOC-02, banco de examen escrito de 3 ítems/9 variantes para C-N9-02, banco de 3 arquitecturas ajenas para C-N9-04, banco práctico de 3 variantes para C-N9-03) quedan construidos y verificados; pendiente Paso 6 en adelante (auditoría desde N10, Herencias Declaradas, auditoría adversarial, Informe Final de Nivel) |
| **Autoridad de origen** | DOC-10 §8 (interior de N9, ya aprobado) · DOC-01 (C-N9-01…04) · **candidato a DOC-09** (`docs/investigacion/n9-propuesta-doc09-arquitectura-columna-vertebral.md`, aprobado en visión por el Director 2026-07-21) |
| **Depende de** | DOC-10 v1.0.3 · DOC-00 · DOC-01 · DOC-02 · DOC-12 (íntegro — N9 es DOC-12 casi puro, ver §3.1) · `docs/investigacion/n9-propuesta-doc09-arquitectura-columna-vertebral.md` · `docs/investigacion/n9-decision-entornos-locales-vs-cloud.md` |
| **Produce / desarrolla** | La estructura docente completa de N9: fichas de laboratorio por tema, proyecto de nivel (infraestructura operando la Columna Vertebral), compuerta, y las Herencias Declaradas hacia SYL-N10 |
| **Estándar de calidad** | El mismo de SYL-N1/N2/N3: *"Si otro instructor excepcional impartiera este nivel utilizando únicamente el syllabus, ¿obtendría prácticamente la misma calidad formativa?"* |
| **Historial** | 0.1.0-draft (2026-07-21): Documento de Diseño. Precedido por dos documentos de investigación propios: la propuesta de arquitectura DOC-09 (capas L0-L4, puertos, ley de evolución por extensión — aprobada por el Director con 8 principios adicionales) y la decisión de entornos local/cloud por módulo (verificada por búsqueda dirigida: vLLM funciona en WSL2 con GPU passthrough automático sobre la RTX 5070; GPU dentro de `kind`/K8s es frágil y se declara fuera de alcance obligatorio con razón técnica citada — M1 y M2 se mantienen desacoplados a nivel de GPU, comunicados por red, arquitectónicamente correcto según DOC-09 L2). Bibliografía oficial de DOC-10 verificada por WebFetch/WebSearch real (Kleppmann DDIA 2nd ed., MLOps Zoomcamp de DataTalksClub, docs oficiales de Kubernetes y vLLM, incluido el `benchmarks/benchmark_serving.py` oficial del propio repo de vLLM para medición real de M2). · **0.2.0 (2026-07-22): Instrucción directa del Director — densidad redefinida y estándar de calidad elevado.** (a) Cada **módulo** completo (no cada laboratorio) es ahora un solo "tema" de **15-20 días** con **~25-40 ejercicios reales**, manteniendo el esquema técnico DOC-12 (`modalidad:"real"`, 13 secciones) pero con la unidad de contenido renombrada de "Laboratorio N" a "Día N" para reflejar la densidad exigida. (b) Estándar de calidad elevado explícitamente a nivel MIT/Stanford/CMU/Berkeley: cada tema exige investigación real previa (no solo documentación oficial — papers académicos, artículos de las propias autoridades de cada tecnología), una sección explícita de "cuándo NO usar esta herramienta" con alternativas reales y trade-offs, y al menos un ejercicio de análisis/comparación/justificación de arquitectura por día (no solo ejecución de comandos). **M1 (Orquestación a escala) reconstruido íntegramente bajo este estándar: 18 días, 40 ejercicios, verificado en su totalidad contra un clúster kind real** — ver §5. M2-M4 se construirán con el mismo modelo de densidad (15-20 días/módulo) cuando les llegue el turno. · **0.3.0 (2026-07-22): M2 (Model serving) completo — 16 días, 33 ejercicios, verificado en su totalidad contra vLLM real sirviendo sobre la RTX 5070 del Director vía WSL2.** Incluye la instalación real de vLLM con 5 fallos reales encadenados diagnosticados y resueltos (memoria pinned desactivada por defecto en WSL2, compilador C faltante, CUDA Toolkit/nvcc faltante — no solo el runtime que trae PyTorch), un modelo real de 7B parámetros cuantizado en AWQ sirviendo con ~95% de la VRAM real en uso, medición oficial con `vllm bench serve` (hallazgo real: `benchmarks/benchmark_serving.py`, citado en la bibliografía original de este documento, está deprecado — el propio proyecto lo reemplazó por un comando de CLI integrado desde que se investigó la bibliografía hasta que se construyó el contenido), comparación cuantitativa real vLLM vs. adaptador ingenuo (~57× de throughput, medido, no citado), curva real de saturación por concurrencia, Ray Serve real (incluida su integración con vLLM y autoescalado por métricas de cola en vez de CPU), y la integración real de `VLLMProvider` (contrato DOC-09) conectando el clúster de M1 con el servidor vLLM externo vía `host.docker.internal`, verificada con una petición HTTP real cruzando esa frontera. · **0.4.0 (2026-07-22): M3 (Datos en movimiento) completo — 16 días, 33 ejercicios, verificado en su totalidad contra Redpanda real** (un broker y, para las pruebas de fallo, un clúster real de 3 vía Docker Compose). Incluye particionado real con colisión de hash verificada, garantías de entrega con un duplicado real provocado (at-least-once), un experimento real completo de fallo de líder (EPOCH 1→2, mensaje pre-fallo íntegro, recuperación confirmada), Schema Registry real (incluido de fábrica en Redpanda, con un cambio incompatible rechazado en vivo), un día dedicado a criterio de arquitectura ("cuándo NO usar Kafka/Redpanda", mismo principio que M1.Día 1), y la integración real de `RedpandaEventBus` (contrato DOC-09) con Redpanda desplegado como StatefulSet DENTRO del clúster de M1 (a diferencia de vLLM en M2, sin necesidad de GPU) — incluido un hallazgo real de discrepancia de diagnóstico DNS (`nslookup` falla en el nombre corto de un Pod de StatefulSet, pero una conexión TCP real con el mismo nombre funciona). · **0.5.0 (2026-07-22): M4 (Observabilidad y escalabilidad) completo — 16 días, 32 ejercicios, verificado en su totalidad contra un stack real de Prometheus + Grafana + Alertmanager.** Incluye instrumentación real con `prometheus_client` y un hallazgo real de API (Grafana 13.1.0 requiere endpoints basados en UID, no ID numérico, para health checks de datasource), observación real de M1 (`kube-state-metrics`) y M2 (métricas nativas de vLLM, `histogram_quantile` para P99 de TTFT), vocabulario SRE de Google (SLI/SLO/error budget) aplicado con datos propios y error budget real calculado, ciclo de vida completo de una alerta real verificado (`pending→firing→resolved`, incluida una demostración real de fatiga de alertas por un `for:` mal calibrado), correlación real de métrica anómala a causa exacta usando el `trace_id` propagado desde M1.Día 1, un modelo FinOps real comparando el costo real de la RTX 5070 (capital amortizado + electricidad medida) contra precios reales de nube 2026 con break-even calculado, la Universal Scalability Law (ya usada en M1.Día 17) reajustada con datos reales de contención de GPU compartida, la segunda implementación real del puerto `TelemetrySink` (`PrometheusTelemetrySink`, sustituida en componentes reales sin tocar su lógica, con prueba formal de sustituibilidad), un dashboard unificado real exportado como JSON versionable, la competencia C-N9-04 verificada con el diagnóstico real de un sistema ajeno nunca visto antes, y el cierre integral de N9 con demostración en vivo de los 4 módulos simultáneos y defensa escrita de 6 decisiones arquitectónicas clave. **Con esto, los 4 módulos de N9 (66 días, 138 ejercicios) quedan construidos y verificados íntegramente.** · **0.6.0 (2026-07-22): Paso 4 completo — Capstone ET9 construido** (`n9et9`, objeto real `modalidad:"proyecto"`, último elemento de `LEVEL9`, esquema verificado contra `n1et1`/`n3et3`). 6 hitos integran los 4 módulos en una sola Columna Vertebral real operando de extremo a extremo: verificación del contrato DOC-09 sobre el sistema heredado de N7/N8 (con la advertencia estructural de construcción en paralelo de la guía maestra §13.7 aplicada honestamente — declara stub si N7/N8 aún no estaban congelados), HPA medido antes/después con USL sobre M1, `VLLMProvider` real con benchmark propio sobre M2, migración real a `RedpandaEventBus` con throughput medido sobre M3, dashboard unificado + informe FinOps + diagnóstico C-N9-04 sobre M4, y una demostración final en vivo de una petición real atravesando los 4 componentes. Defensa de 5 preguntas siguiendo el estándar de tres momentos de N1 más la pregunta de validación final estándar desde N2. · **0.7.0 (2026-07-22): Paso 5 completo — compuerta construida.** Instrumentos asignados directamente por la matriz ya sellada de DOC-02 §4 (no redefinidos): C-N9-01 vía RM-06/TP-01 (el propio Capstone ET9), C-N9-02 vía RM-02+RM-05 (banco escrito nuevo + defensa del Capstone ya construida), C-N9-03 vía RM-03 (banco práctico nuevo, ejecutado en vivo sobre hardware real), C-N9-04 vía RM-02 (banco de 3 arquitecturas ajenas anonimizadas, cada una con riesgos reales trazables a una lección específica del nivel, nunca un caso de juguete). Hallazgo de la revisión: C-N9-02 es la única competencia de N9 sin módulo dedicado (se construye por acumulación entre M1/M3/M4) — el banco de examen fue diseñado explícitamente para cubrir sus 3 dimensiones (réplicas, colas, costes) con un ítem cada una, no por casualidad. Todos los valores numéricos del banco (break-even FinOps, cálculos de réplicas/particiones) verificados por ejecución real antes de fijarse. Sigue: Paso 6 (auditoría desde N10 + Herencias Declaradas) en adelante. |

---

## 1. Tabla resumen

| Módulo | Días | Ejercicios | Competencias | Gran pregunta del módulo |
|---|---|---|---|---|
| M1 · Orquestación a escala | **18** ✅ construido | **40** | C-N9-01 | ¿Cómo mantiene un sistema su forma deseada cuando las máquinas y los procesos fallan constantemente? |
| M2 · Model serving | **16** ✅ construido | **33** | C-N9-03 | ¿Qué separa "un LLM que responde" de "un LLM que sirve tráfico real de producción"? |
| M3 · Datos en movimiento | **16** ✅ construido | **33** | C-N9-02 (parcial) | ¿Qué pasa cuando quien produce datos y quien los consume no pueden — o no deben — hablar directamente? |
| M4 · Observabilidad y escalabilidad | **16** ✅ construido | **32** | C-N9-02, C-N9-04 | ¿Cómo sabes que un sistema está sano sin tener que preguntarle a un usuario si algo falló? |
| Proyecto de nivel (Capstone ET9) | **6 hitos** ✅ construido | — | Todas | Integración: operar, con mediciones propias, la infraestructura completa de la Columna Vertebral |

*Densidad redefinida 2026-07-22 (instrucción directa del Director, ver Historial): cada MÓDULO completo es un solo "tema", de 15-20 días, no cada laboratorio individual. Sigue siendo DOC-12 (cada "día" es un objeto `modalidad:"real"` autocontenido de 13 secciones, ~90-140 min), pero renombrado de "Laboratorio N" a "Día N" para reflejar la densidad. M1 ya construido y verificado con esta densidad: 4 bloques (A: fundamentos, Días 1-4 · B: Deployments/Services/Ingress, Días 5-9 · C: configuración/estado/rollouts, Días 10-14 · D: autoescalado y cierre, Días 15-18).*

*Nota metodológica sobre las ~400h/~4 meses de DOC-10: incluyen, igual que N3 lo hizo explícito, el consumo real de la bibliografía externa citada (capítulos relevantes de Kleppmann Parte I-II, los módulos 3-5 de MLOps Zoomcamp) — el contenido que la Academia construye en el Campus andamia y verifica esas horas, no las sustituye.*

## 2. Identidad del nivel

Por referencia a DOC-10 §8: **N9 · Sistemas Distribuidos** no enseña a construir sistemas de IA nuevos — enseña a **operar** los que N7/N8 ya construyeron, con el rigor de infraestructura de producción real. Entrada: N8 Superado (cadena técnica real: N9 asume que existe algo desplegable con el contrato de DOC-09 §7, no conocimiento nuevo de N8 en sí). Salida: examen + proyecto + defensa (incluye diagnóstico de una arquitectura ajena, C-N9-04) → habilita N10 (IA Local), que reemplazará el `LLMProvider` de nube/GPU-remota por uno local optimizado, sin tocar nada de lo que N9 construyó.

Si N7 enseñó a razonar con datos y N8 a razonar con planes y herramientas, N9 enseña **la disciplina de que ese razonamiento siga funcionando cuando el tráfico crece, una máquina falla a las 3am, o un componente se degrada silenciosamente** — el tránsito de "funciona en mi laptop" a "funciona aunque yo no esté mirando".

## 3. Principios de ejecución

1. **Principio "100% DOC-12"** *(a diferencia de N3 — decisión arquitectónica explícita, no un descuido)*: aplicando el criterio de frontera de DOC-12 §5, N9 responde "sí sale del navegador" en los tres criterios — todo el nivel depende de terminal propia (WSL2), Docker, cuentas (ninguna de pago obligatoria), y estado persistente real. No hay contenido Pyodide en N9 más allá de menciones conceptuales acotadas si algún laboratorio lo justifica puntualmente.
2. **Arquitectura de referencia obligatoria**: todo laboratorio de N9 se diseña y se redacta en términos de las capas y puertos de DOC-09 (`docs/investigacion/n9-propuesta-doc09-arquitectura-columna-vertebral.md`) — M1/M3/M4 nunca tocan L0/L1, solo añaden implementaciones L2 e infraestructura L3/L4. Cada laboratorio de cierre de módulo declara explícitamente qué puerto conecta o sustituye (ver tabla DOC-09 §6).
3. **Diseño para el hardware real del estudiante** *(instrucción directa del Director)*: RTX 5070 (12GB VRAM) · Ryzen 5 9600X · 32GB DDR5. Ningún laboratorio se simplifica para "correr en cualquier PC" — se diseña para sacar partido real de este equipo (modelos cuantizados que llenan la VRAM disponible, clústeres `kind` multi-nodo reales, stack completo Prometheus+Grafana+Redpanda+K8s conviviendo en 32GB). Cuando una práctica exceda el equipo, se documenta la limitación y se propone la alternativa más cercana — nunca se reduce el nivel conceptual (ver `n9-decision-entornos-locales-vs-cloud.md`).
4. **GPU desacoplada de K8s, por razón técnica verificada, no por atajo**: M1 y M2 se comunican por red, nunca por planificación de GPU dentro de `kind` — hallazgo verificado por búsqueda dirigida (2026): la exposición de GPU como recurso planificable dentro de Kubernetes sobre Docker Desktop/WSL2 es frágil y requiere NVIDIA GPU Operator, declarado explícitamente Beyond the Curriculum. Este es, en sí mismo, un patrón real de producción (nodos GPU dedicados servidos aparte del clúster de aplicación), no una simplificación oculta.
5. **Error inducido en vivo, en todo laboratorio, sin excepción** (DOC-12 §2.5bis, no negociable): M1 mata un Pod y un nodo; M2 satura la cola de vLLM hasta ver degradación de latencia; M3 mata el broker líder de Redpanda; M4 induce una alerta real. Cada uno se conecta directamente a la tabla de diagnóstico de 5 columnas de su laboratorio (DOC-12 §2.8).
6. **Verificación empírica antes de fijar cualquier paso o comando** (guía maestra §9, aplicada a entorno real en vez de a `check()`): cada comando, cada resultado esperado y cada valor de referencia de N9 se ejecuta de verdad en WSL2/Docker/`kind` antes de escribirse en el laboratorio — nunca se documenta de memoria ni por analogía con otro proyecto.
7. **Integración progresiva y acumulativa**: el laboratorio de cierre de cada módulo conecta explícitamente con el/los módulo(s) anteriores — M2 se sirve como servicio externo al clúster de M1; M3 alimenta la ingesta que M1 ya orquesta; M4 observa M1+M2+M3 simultáneamente y cierra el nivel con el diagnóstico de una arquitectura ajena (C-N9-04) y el modelo de costes con datos propios (C-N9-02).
8. **Herencia entrante formal de N7/N8** (DOC-09 §7): un servicio L3 de un solo proceso, contrato `POST /query` / `POST /ingest` / `GET /health`, con telemetría ya emitida desde su primer pipeline funcional. Si esa versión real no existe al construir el proyecto de N9, se levanta un stub mínimo que cumple exactamente ese contrato — declarado explícitamente como supuesto de trabajo, sin bloquear el avance (mismo principio que ya autoriza `docs/mision-n9.md`).

## 4. Estructura y grafo local

Troncal: `M1 → M2 → M3 → M4`, con M2 y M3 pudiendo construirse en paralelo conceptual (ambos dependen solo de M1 — necesitan un clúster donde vivir la parte que sí es orquestable — y no dependen duramente entre sí), pero **la implementación en el Campus sigue una secuencia lineal fija M1→M2→M3→M4** (mismo principio de "recorrido lineal único" institucionalizado desde N3 §4, EVT-034): M2 antes que M3 porque servir el modelo es la pieza más visible/motivadora inmediatamente después de tener un clúster; M4 al final porque necesita M1+M2+M3 reales para observar algo genuino.

**Nota de alcance:** M1 y M2 no tienen dependencia dura entre sí a nivel de puerto (DOC-09 §6) — se enseñan en este orden por currículo, no porque M2 técnicamente requiera M1 terminado.

## 5. Fichas pedagógicas por laboratorio

*Plantilla adaptada de DOC-12 §1 (ficha de control) para nivel de planificación — el laboratorio completo (13 secciones) se escribe en el Paso 2, no aquí. Los 5 estándares narrativos de N1-N3 se adaptan a entorno real: **gran pregunta** (universal) · **el problema que se rompe sin esto** (equivalente DOC-12 de "el supuesto que destruye") · **la limitación humana que compensa** · **el error inducido en vivo** (reemplaza "quiebre de intuición" — en DOC-12 el quiebre no es conceptual sino operativo: el momento exacto en que algo falla de verdad frente al estudiante) · **el puerto DOC-09 que conecta o sustituye**.*

### M1 · Orquestación a escala (Kubernetes) — ✅ CONSTRUIDO Y VERIFICADO (18 días, 40 ejercicios)

> **Gran pregunta del módulo: ¿cómo mantiene un sistema su forma deseada cuando las máquinas y los procesos fallan constantemente?**

*Construido íntegramente en `index.html` (`LEVEL9`, ids `n9m1t1`…`n9m1t1r`), cada día verificado contra un clúster `kind` real antes de escribirse (guía maestra §9). Cada día incluye, sin excepción: cita académica/profesional real (no genérica), una sección "cuándo NO usar esta herramienta" con alternativas y trade-offs, y ≥1 ejercicio de comparar/justificar/decidir arquitectura, además del error inducido en vivo (DOC-12 §2.5bis).*

**Bloque A — Fundamentos (Días 1-4):** D1 *Del proceso único al clúster* (Borg/Kubernetes — Verma et al. 2015, Burns et al. CACM 2016; cuándo NO usar K8s — Nomad/ECS/Cloud Run/un solo servidor, con trade-offs reales; primer clúster `kind` multi-nodo) · D2 *Pods a fondo: ciclo de vida y las tres pruebas de salud* (readiness/liveness/startupProbe, verificadas en vivo; el fenómeno real de "flapping" y su mitigación) · D3 *El límite de un proceso: PID 1, namespaces y por qué SIGKILL no siempre mata* (verificado dos veces con timestamp exacto; cita primaria `man 7 pid_namespaces`; zombies y `tini` como solución profesional real) · D4 práctica integradora.

**Bloque B — Deployments/Services/Ingress (Días 5-9):** D5 *Declarar el estado deseado* (Deployment→ReplicaSet, con Deployment/StatefulSet/DaemonSet/Job comparados) · D6 *Self-healing cronometrado* (metodología formal de chaos engineering — Basiri et al./Netflix 2017 — con hipótesis, fallo inyectado y resultado medido) · D7 *Services* (kube-proxy/CoreDNS, los 4 tipos de Service comparados, cuándo un service mesh es necesario) · D8 *Exponer tráfico HTTP real: de Ingress a Gateway API* (hallazgo de actualidad verificado: `ingress-nginx` retirado oficialmente en marzo 2026; Gateway API como sucesor real, con controlador nginx real desplegado y enrutamiento por path verificado con `curl`) · D9 práctica integradora.

**Bloque C — Configuración y estado (Días 10-14):** D10 *ConfigMaps* (Twelve-Factor App Factor III, Wiggins/Heroku 2011) · D11 *Secrets: por qué "Secret" no significa "protegido"* (decodificación real verificada; CIS Benchmark; el matiz real de que External Secrets Operator NO resuelve el cifrado en reposo por sí solo) · D12 *Almacenamiento persistente* (PVC/StorageClass/WaitForFirstConsumer verificados; límite real de `local-path-provisioner` vs. CSI/Rook-Ceph en producción) · D13 *Rollouts y rollback* (rollout exitoso ~11s y roto+rollback ~1s, ambos cronometrados y verificados; RollingUpdate vs. Blue-Green vs. Canary, con Argo Rollouts como herramienta real) · D14 práctica integradora.

**Bloque D — Autoescalado y cierre (Días 15-18):** D15 *Autoescalado real: el HPA y la pieza que lo hace posible* (fórmula exacta del HPA; `metrics-server` con su fallo real de TLS en `kind` diagnosticado y corregido; KEDA como extensión real para scaling por eventos) · D16 *Construye el servicio de referencia* (contrato DOC-09 §7 implementado en código real, verificado local y cargado a `kind`) · D17 *Mide de verdad* (experimento controlado real: 1 réplica → 4.35 req/s, 5 réplicas → 12.75 req/s — 2.93× no 5×, explicado con la Universal Scalability Law de Gunther, con cálculo real de α a partir de los propios datos) · D18 *Cierre de M1* — sistema completo integrado + defensa escrita completa.

**Puerto DOC-09:** M1 no toca L0/L1 en ningún día — el Día 18 lo demuestra explícitamente desplegando el servicio de referencia (contrato L3) sin ninguna modificación a su lógica interna.

### M2 · Model serving (vLLM, Ray) — ✅ CONSTRUIDO Y VERIFICADO (16 días, 33 ejercicios)

> **Gran pregunta del módulo: ¿qué separa "un LLM que responde" de "un LLM que sirve tráfico real de producción"?**

*Construido íntegramente en `index.html` (`LEVEL9`, ids `n9m2t1`…`n9m2t1p`), cada día verificado contra vLLM real sirviendo sobre la RTX 5070 del Director vía WSL2 — no un stub ni una simulación.*

**Bloque A — Fundamentos de serving (Días 1-4):** D1 *El problema real detrás de servir un LLM* (PagedAttention — Kwon et al. SOSP 2023; instalación real con 5 fallos reales encadenados diagnosticados hasta su causa raíz: memoria pinned desactivada por defecto en WSL2, compilador C faltante, CUDA Toolkit/nvcc faltante) · D2 *PagedAttention y memoria* (KV cache real de la GPU del Director leída de los logs de arranque, cálculo real de cuántas conversaciones caben) · D3 *Cuantización real* (GPTQ/AWQ/GGUF comparados con evidencia; modelo real de 7B-AWQ sirviendo con ~95% de la VRAM real en uso, respondiendo correctamente en español) · D4 práctica integradora.

**Bloque B — Medición real (Días 5-8):** D5 *Medir de verdad* (hallazgo real verificado en vivo: `benchmark_serving.py`, citado en la bibliografía original de este documento, está deprecado — reemplazado por `vllm bench serve`; TTFT/TPOT/ITL explicados) · D6 *vLLM vs. un adaptador ingenuo* (comparación real: ~57× de throughput medido, no citado — 0.52 req/s ingenuo vs. 29.69 req/s vLLM, mismo modelo, misma GPU) · D7 *Punto de saturación* (curva real de 5 niveles de concurrencia: escalado casi lineal 1→32, saturación real medida entre 128→256) · D8 práctica integradora.

**Bloque C — Ray Serve (Días 9-12):** D9 *Ray Serve* (por qué existe además de Kubernetes/HPA de M1; réplicas reales confirmadas con PIDs distintos, no supuestas) · D10 *Ray Serve + vLLM* (réplicas reales de un modelo real, costo real de VRAM medido) · D11 *Autoescalado con Ray Serve* (métricas nativas de cola vs. CPU del HPA de M1) · D12 práctica integradora.

**Bloque D — Integración con la Columna Vertebral (Días 13-16):** D13 *VLLMProvider* (contrato DOC-09 §2/§4 implementado en código real, verificado con FakeProvider intercambiable) · D14 *VLLMProvider entra al clúster* (arquitectura desacoplada de M1 §3.4 verificada con una petición HTTP real cruzando la frontera clúster↔host vía `host.docker.internal`) · D15 *Degradación bajo carga concurrente* (4 "agentes" simulados reales compitiendo por la misma GPU, medido) · D16 *Cierre de M2* — sistema completo integrado + defensa escrita completa.

**Puerto DOC-09:** primera implementación real de `LLMProvider` (`VLLMProvider`, Día 13) fuera de una API de nube, conectada en producción a la Columna Vertebral (Día 14) — el sistema real, no un stub, sirviendo inferencia desde infraestructura propia del Director.

### M3 · Datos en movimiento (colas, Kafka) — ✅ CONSTRUIDO Y VERIFICADO (16 días, 33 ejercicios)

> **Gran pregunta del módulo: ¿qué pasa cuando quien produce datos y quien los consume no pueden — o no deben — hablar directamente?**

*Construido íntegramente en `index.html` (`LEVEL9`, ids `n9m3t1`…`n9m3t1p`), cada día verificado contra Redpanda real en Docker (un solo broker, y un clúster real de 3 brokers vía Docker Compose para las pruebas de fallo).*

**Bloque A — Fundamentos de colas (Días 1-4):** D1 *Por qué una cola* (Redpanda real en Docker, primer productor/consumidor, persistencia verificada sin consumidor activo) · D2 *Particiones* (asignación real por key verificada, incluida una colisión de hash real con solo 3 keys/3 particiones) · D3 *Grupos de consumo* (rebalanceo real observado al agregar un segundo consumidor, límite real consumidores≤particiones) · D4 práctica integradora.

**Bloque B — Garantías y fallos reales (Días 5-8):** D5 *Garantías de entrega* (at-most-once/at-least-once/exactly-once — Kleppmann Cap. 11; duplicado real provocado y verificado, no teórico) · D6 *Replicación y fallo real* (clúster real de 3 brokers, líder real matado, elección observada con EPOCH 1→2, mensaje pre-fallo confirmado íntegro, recuperación verificada) · D7 *Offsets y replay* (reprocesamiento real de eventos históricos con `rpk group seek`) · D8 práctica integradora.

**Bloque C — Ecosistema y trade-offs (Días 9-12):** D9 *Redpanda/Kafka vs. alternativas* (RabbitMQ, SQS/Pub-Sub, Pulsar — cuándo NO usar un log distribuido, mismo principio de M1.Día 1) · D10 *Schema Registry* (real, incluido de fábrica en Redpanda; cambio incompatible real rechazado en vivo) · D11 *Procesamiento de streams* (los 4 tipos de ventana de Kleppmann Cap. 11, implementados en miniatura) · D12 práctica integradora.

**Bloque D — Integración con la Columna Vertebral (Días 13-16):** D13 *RedpandaEventBus* (contrato DOC-09 §2/§6 del puerto `EventBus`, verificado intercambiable con `InProcessEventBus`) · D14 *RedpandaEventBus entra al clúster* (Redpanda como StatefulSet real DENTRO de `kind` —a diferencia de vLLM en M2, sin necesidad de GPU—, con un hallazgo real de discrepancia DNS: `nslookup` falla en el nombre corto pero una conexión TCP real con el mismo nombre funciona) · D15 *Medición honesta* (ingesta síncrona vs. asíncrona, con la limitación real del método reconocida explícitamente) · D16 *Cierre de M3* — sistema completo integrado + defensa escrita completa.

**Puerto DOC-09:** `EventBus` conectado en producción (`RedpandaEventBus`, Día 13-14) — la ingesta ya no bloquea al productor, verificado con Redpanda real corriendo dentro del mismo clúster de Kubernetes que M1 orquesta.

### M4 · Observabilidad y escalabilidad ✅ construido y verificado (2026-07-22)

> **Gran pregunta del módulo: ¿cómo sabes que un sistema está sano sin tener que preguntarle a un usuario si algo falló?**

**Bloque A — Fundamentos de observabilidad (Días 1-4):** D1 *Los 3 pilares* (logs/métricas/trazas, primer servicio real instrumentado con `prometheus_client`, Prometheus+Grafana reales desplegados y conectados, hallazgo real de API: Grafana 13.1.0 requiere UID —no ID numérico— para health checks de datasource) · D2 *Observa M1* (`kube-state-metrics` real desplegado sobre `kind`, consulta PromQL real que detecta un Deployment con réplicas no disponibles sin nombrarlo) · D3 *Observa M2* (métricas nativas de vLLM activadas y consultadas, `histogram_quantile` real para P99 de TTFT, comparado contra la medición manual de M2) · D4 práctica integradora (dashboard único con paneles de servicio propio + K8s + vLLM, diagnóstico exclusivo desde dashboard).

**Bloque B — SLOs, alertas y correlación (Días 5-8):** D5 *SLIs y SLOs* (vocabulario SRE de Google aplicado con datos reales, error budget real calculado, análisis crítico de por qué 99.99% no es realista para un solo nodo sin redundancia) · D6 *Alertas reales* (Alertmanager real desplegado, ciclo de vida completo `pending→firing→resolved` verificado con datos reales, fatiga de alertas demostrada en vivo con un `for:` mal calibrado) · D7 *Correlación real* (trace_id propagado desde M1.Día 1 usado para saltar de una métrica anómala al log exacto, demostración de la imposibilidad práctica de correlacionar sin trace_id bajo concurrencia real) · D8 práctica integradora (ciclo completo SLO→alerta→disparo→correlación de memoria).

**Bloque C — FinOps y escalabilidad real (Días 9-12):** D9 *FinOps: local vs. nube* (modelo de costos real de la RTX 5070 —capital amortizado + electricidad medida— comparado contra precios reales de nube 2026, break-even calculado en horas, decisión de compra distinta según patrón de uso) · D10 *Costo real de la Columna Vertebral completa* (modelo multi-componente agregado, costo real por petición usando throughput medido en M2) · D11 *Universal Scalability Law aplicada al sistema completo* (mismo modelo de M1.Día 17, ahora ajustado a la contención real de una GPU física compartida, predicción contrastada contra un resultado real) · D12 práctica integradora (caso de negocio completo costo+escalabilidad, plan de escalamiento a 3 fases).

**Bloque D — Integración final y cierre de nivel (Días 13-16):** D13 *`PrometheusTelemetrySink`* (segunda implementación real del puerto `TelemetrySink`, sustituida en componentes reales de M1/M2 sin tocar su lógica, prueba formal de sustituibilidad vía test automatizado) · D14 *Dashboard unificado real* (golden signals de Google SRE diseñados por escrito antes de construir, dashboard exportado como JSON versionable) · D15 *C-N9-04: diagnóstico de arquitectura ajena* (sistema de terceros nunca visto antes, diagnosticado exclusivamente con las herramientas del módulo, hipótesis inicial incorrecta documentada honestamente con la evidencia que la descartó) · D16 *Cierre de M4 y defensa integral de N9* — demostración en vivo de M1+M2+M3+M4 simultáneos, defensa escrita de 6 decisiones arquitectónicas clave con sus alternativas descartadas, auto-evaluación honesta contra las 4 preguntas de aceptación institucional.

**Puerto DOC-09:** `TelemetrySink` — segunda implementación real (`PrometheusTelemetrySink`, Día 13), verificada intercambiable con `ConsoleTelemetrySink` sin modificar el código consumidor, con test formal de sustituibilidad (Ejercicio 26).

**Nota de honestidad metodológica:** dado que N9 es 100% local/gratuito (§3.3), el "coste" de FinOps (Días 9-10) es siempre proyectado a partir de utilización real medida (consumo eléctrico real vía `nvidia-smi`, throughput real vía `vllm bench serve`) extrapolada contra precios públicos vigentes de nube (2026) — nunca gasto real ni factura real, declarado explícitamente en el propio contenido.

## 6. Proyecto de nivel (capstone) — ✅ construido (Paso 4, 2026-07-22)

*Objeto real `n9et9` ("Capstone ET9 — La Columna Vertebral que puedes operar, no solo demostrar"), último elemento de `LEVEL9` en `index.html`, `modalidad:"proyecto"` (esquema §4 de la guía maestra, verificado contra el capstone real de N1/`n1et1` y N3/`n3et3`).*

- **Objetivo:** integrar los 4 módulos en una única Columna Vertebral real y operable — un clúster K8s (M1) orquestando un servidor de inferencia GPU real (M2), conectado a un bus de eventos real (M3), con observabilidad real de extremo a extremo (M4) — implementando formalmente el contrato DOC-09 sobre el sistema heredado de N7/N8. Certifica "puede operarlo, escalarlo con datos, diagnosticarlo bajo presión, y defenderlo", nunca "logró que funcionara una vez".
- **6 hitos reales:** h1 contrato DOC-09 verificado sobre el sistema heredado de N7/N8 (real o stub declarado explícitamente, con la advertencia estructural de construcción en paralelo de la guía maestra §13.7 aplicada honestamente) · h2 M1 — clúster + HPA medido antes/después bajo carga real (USL) · h3 M2 — `VLLMProvider` real sirviendo, con un benchmark propio y justificado (no repetido de M2) · h4 M3 — un flujo real migrado a `RedpandaEventBus`, throughput medido antes/después de la migración · h5 M4 — dashboard unificado con los 4 componentes simultáneos, informe FinOps real, diagnóstico real de arquitectura ajena (C-N9-04) · h6 integración final — demostración en vivo de una petición real atravesando M1→M2→M3→M4, historia de Git revisada y honesta.
- **Fuera de alcance (declarado explícitamente):** alta disponibilidad real (una sola RTX 5070, sin failover) · un segundo adaptador real por puerto más allá de los ya construidos día a día · despliegue real en nube (el análisis de costo de M4.Día 9-10 es siempre proyectado, nunca desplegado).
- **Flujo de Git:** sí aplica (DOC-12, a diferencia de N3) — repositorio propio del proyecto, un commit real por hito, sin `push --force`, historia auditable en la defensa.
- **Defensa:** 5 preguntas siguiendo el estándar institucional de tres momentos de N1 (predicción de preguntas de un evaluador senior, mejor decisión con evidencia real, madurez de qué cambiaría) más una pregunta de limitación honesta específica de infraestructura de un solo nodo, y la pregunta de validación final estándar de la Academia desde N2 ("¿confiarías en que este sistema sostendría tráfico real?").

## 7. Compuerta — ✅ construida (Paso 5, 2026-07-22)

### 7.1 Instrumentos — asignados por DOC-02 §4 (matriz ya sellada, no redefinida aquí)

| Competencia | Evidencia | Instrumento | Norma | Checklist |
|---|---|---|---|---|
| C-N9-01 (orquestación + observabilidad) | [A] Proyecto | **RM-06** | DOC-02 | TP-01 |
| C-N9-02 (trade-offs de escalabilidad — colas/réplicas/costes, con mediciones propias) | [E][D] Escrito + oral | **RM-02, RM-05** | DOC-02 | — |
| C-N9-03 (sirve modelos midiendo latencia/throughput, mejora antes/después) | [A][E] Práctico (mediciones) | **RM-03** | DOC-02 | — |
| C-N9-04 (diagnóstico de arquitectura ajena) | [E] Escrito (diagnóstico) | **RM-02** | DOC-02 | — |

**Cobertura de competencias — verificación explícita:**

| Competencia | Verificada por |
|---|---|
| C-N9-01 | M1 completo (18 días) + M4 completo (observabilidad, 16 días) + Capstone ET9 completo (RM-06/TP-01, hitos h2 y h6) |
| C-N9-02 | M1 (HPA/USL, Bloque D) + M3 (particiones/colas, Bloque A-B) + M4 (FinOps/USL reajustada, Bloque C) + banco de examen §7.3 (RM-02) + Capstone hitos h2/h4/h5 + defensa del Capstone (RM-05, ya construida en `n9et9.evalu.defensa`) |
| C-N9-03 | M2 completo (16 días) + banco de examen §7.4 (RM-03, práctico) + Capstone hito h3 |
| C-N9-04 | M4.Día 15 completo + banco de arquitecturas §7.3.2 (RM-02) + Capstone hito h5 |

**Hallazgo de la revisión:** a diferencia de C-N3-01…04 (mapeo 1:1 módulo↔competencia), C-N9-02 es la única competencia de N9 sin un módulo dedicado — se construye por acumulación deliberada across M1 (réplicas), M3 (colas) y M4 (costes), exactamente como DOC-01 la describe ("colas, réplicas, costes"). Esto exige que el banco de examen de C-N9-02 (§7.3.1) cubra las tres dimensiones explícitamente, no solo una — verificado abajo: los 3 ítems del banco cubren réplicas (Ítem 1), colas (Ítem 2) y costes (Ítem 3) por diseño, no por casualidad. RM-05 (oral) para C-N9-02 no requiere un banco separado: la defensa del Capstone ET9 (`n9et9.evalu.defensa`, ya construida) ya sondea directamente las decisiones de escalabilidad del sistema completo — reutilizarla evita duplicar el mismo tipo de evidencia en dos instrumentos distintos.

### 7.2 Umbral y trazabilidad

Compuerta de nivel: **examen** (RM-02 + RM-03, banco rotable ≥3 variantes/ítem, NNR-02) + **Capstone ET9 contra TP-01 íntegra** (RM-06) + **defensa** (RM-05, vía `n9et9.evalu.defensa`) → certificación por la Plataforma. Umbral: el vigente de DOC-00 §17.3.3 (no restatado aquí, DOC-02 §1). Veredicto trazable completo (17.2.4) en cada acto, con la rúbrica y su versión (RM-02 v1.0, RM-03 v1.0, RM-05 v1.0, RM-06 v1.0) estampada.

### 7.3 Banco de examen escrito (RM-02) — ítems rotables (≥3 variantes por ítem, NNR-02)

#### 7.3.1 C-N9-02 · trade-offs de escalabilidad, con mediciones propias

**Ítem 1 (réplicas — Little's Law/USL, M1.Bloque D).** "Dado un throughput de llegada λ y un throughput de servicio μ por réplica, calcula el número mínimo de réplicas para que la utilización ρ no supere el 80% — el mismo umbral bajo el cual M1.Día 15-17 mantuvo la latencia estable. Explica por qué acercarse a ρ=1 degrada la latencia de forma NO lineal, no solo proporcional."
- Variante A: λ=45 req/s, μ=12 req/s/réplica → N ≥ 45/(12×0,8) = 4,6875 → **5 réplicas**.
- Variante B: λ=100 req/s, μ=8 req/s/réplica → N ≥ 100/(8×0,8) = 15,625 → **16 réplicas**.
- Variante C: λ=20 req/s, μ=25 req/s/réplica → N ≥ 20/(25×0,8) = 1,0 → **1 réplica** (ya en el límite exacto del umbral, sin margen — buen caso para discutir cuándo escalar es prematuro).

**Ítem 2 (colas/particiones — M3.Bloque A).** "Con este throughput de productor y este throughput de consumidor por partición, calcula el número mínimo de particiones para que el consumidor no acumule backlog indefinidamente. ¿Qué pasa si agregas MÁS particiones que consumidores disponibles en el grupo?"
- Variante A: productor 500 msg/s, consumidor 60 msg/s/partición → particiones ≥ 500/60 = 8,33 → **9 particiones**. Particiones sin consumidor asignado: overhead de metadata, cero beneficio de paralelismo.
- Variante B: productor 1200 msg/s, consumidor 150 msg/s/partición → particiones ≥ **8** (exacto).
- Variante C: productor 90 msg/s, consumidor 100 msg/s/partición → particiones ≥ 90/100 = 0,9 → **1 partición** (el consumidor ya procesa más rápido de lo que llega — escalar particiones aquí no resuelve ningún problema real).

**Ítem 3 (costes — FinOps, M4.Bloque C).** "Dado este costo real local por hora (capital amortizado + electricidad) y este precio real de nube equivalente, calcula el break-even en horas de uso, y decide si el patrón de uso dado justifica la inversión en hardware local."
- Variante A: costo local $0,045/h, nube g6.xlarge $0,8048/h, inversión $549 → break-even ≈ **723 horas** (~90 días a 8h/día — uso intensivo like N9 lo justifica).
- Variante B: costo local $0,045/h, nube g5.xlarge $1,006/h, inversión $549 → break-even ≈ **572 horas** (~71 días a 8h/día).
- Variante C: mismo hardware, patrón de uso esporádico (2h/semana) contra g6.xlarge → break-even ≈ **6,95 años** — muy por encima de la vida útil real del hardware (~3 años): bajo este patrón, la nube es la decisión económicamente correcta, aunque el hardware local exista y funcione.

#### 7.3.2 C-N9-04 · banco de arquitecturas ajenas (2-3 arquitecturas, anonimizadas y sintéticas, con riesgos reales identificables)

*El examinador presenta UNA arquitectura al azar (nunca las tres). El estudiante produce un diagnóstico escrito: fortalezas reales, riesgos reales, y qué cambiaría primero y por qué — mismo estándar de M4.Día 15, ahora sobre un caso preparado institucionalmente, no uno que el propio estudiante construyó.*

**Arquitectura X — "Motor de recomendaciones de una tienda en línea".** Un monolito Flask síncrono llama directamente a un servicio de inferencia ML sin timeout ni circuit breaker; una única base Postgres sirve tanto las transacciones de compra como las consultas analíticas del equipo de datos; el balanceador usa round-robin simple; los despliegues son manuales por SSH+restart, sin health checks ni rollback automático.
- *Fortalezas reales esperadas en la respuesta:* simplicidad operativa real para un equipo pequeño; bajo costo de coordinación entre servicios (no hay red interna que depurar).
- *Riesgos reales esperados:* la llamada de inferencia sin timeout puede colapsar en cascada todo el camino de la petición bajo carga (M1.Día 2, flapping/health checks); Postgres sirviendo OLTP y OLAP a la vez degrada bajo carga analítica (Kleppmann, bibliografía de M3); cero observabilidad hace indiagnosticable cualquier degradación real (M4.Bloque A); despliegues manuales sin rollback significan que un mal deploy se resuelve reaccionando, no revirtiendo.

**Arquitectura Y — "Plataforma de generación de imágenes por etapas".** Un API gateway publica a Kafka; 8 microservicios encadenados procesan cada uno un paso (validar → redimensionar → filtro 1 → filtro 2 → marca de agua → miniatura → notificar → almacenar); cada microservicio mantiene su propio Redis de caché sin estrategia de invalidación compartida; el autoescalado de cada microservicio está configurado solo por CPU.
- *Fortalezas reales esperadas:* cada etapa es desplegable y probable de forma independiente; Kafka aporta durabilidad y replay reales en la frontera de ingestión, donde sí se necesitan.
- *Riesgos reales esperados:* 8 saltos de red encadenados multiplican la latencia de cola (P99 compuesto, no solo la mediana) de forma mucho más severa que el promedio sugiere; autoescalar por CPU un servicio que consume de una cola es la señal equivocada — debería escalar por profundidad de cola/lag de consumidor, exactamente la lección de Ray Serve en M2.Bloque C; cachés independientes sin invalidación compartida son un riesgo de correctness, no solo de rendimiento.

**Arquitectura Z — "Servidor de inferencia LLM que el equipo llama 'listo para producción'".** vLLM real corre en 3 réplicas idénticas detrás de un balanceador round-robin sin afinidad de sesión/KV-cache; cada réplica recarga el modelo completo desde cero en cada arranque; el motor de inferencia usa un adaptador naive sin PagedAttention (decisión deliberada del equipo "para simplificar"); las métricas están expuestas en Prometheus, pero no existe ninguna alerta configurada; no hay ningún registro de costo por petición.
- *Fortalezas reales esperadas:* la replicación es el instinto correcto para disponibilidad; las métricas YA existen, la materia prima de observabilidad está ahí.
- *Riesgos reales esperados:* el balanceo round-robin sin afinidad de KV-cache desperdicia exactamente la optimización que vLLM existe para dar (M2.Día 1-2); sin PagedAttention, la fragmentación de memoria limita el throughput real muy por debajo de lo que el mismo hardware podría sostener; métricas sin alertas es "observabilidad de teatro" — exige que alguien mire un dashboard 24/7, lo que no escala (M4.Día 6); sin costo por petición, el equipo no puede responder si el sistema es económicamente sostenible (M4.Día 9-10).

### 7.4 Banco de examen práctico (RM-03) — ítems rotables, ejecutados sobre hardware real del estudiante

#### C-N9-03 · sirve modelos midiendo latencia/throughput, con mejora antes/después

*El examinador asigna UNA variante. El estudiante configura vLLM real con la configuración subóptima indicada, mide con `vllm bench serve`, aplica la optimización real correspondiente (ya estudiada día a día en M2), vuelve a medir, y reporta la mejora real y porcentual — nunca un número de memoria, siempre ejecutado en vivo sobre su propio hardware.*

- Variante A: sirve con `--gpu-memory-utilization 0.30` (limita artificialmente el espacio de KV-cache) → mide → aplica la corrección de M2.Día 1 (`0.80`) → mide de nuevo → reporta la mejora real de throughput.
- Variante B: sirve un modelo sin cuantizar que cabría cuantizado → mide → aplica cuantización AWQ real (M2.Día 3) → mide de nuevo → reporta la mejora real de VRAM utilizable y throughput.
- Variante C: sirve con `--max-concurrency 1` (serializa todas las peticiones) → mide → aplica la concurrencia óptima real identificada por la USL ajustada (M4.Día 11) → mide de nuevo → reporta la mejora real de throughput agregado.

**Nota de diseño (misma disciplina que N3 §8):** los 3 ítems de C-N9-02 (§7.3.1) cubren las 3 dimensiones exigidas por DOC-01 (réplicas, colas, costes) con 3 variantes cada uno (9 variantes totales); el banco de arquitecturas de C-N9-04 (§7.3.2) tiene 3 arquitecturas completas, cada una con riesgos reales identificables y trazables a una lección específica del nivel — nunca un caso ambiguo o de juguete; los 3 ítems prácticos de C-N9-03 (§7.4) se ejecutan y verifican en vivo, nunca de memoria, siguiendo la misma disciplina de verificación empírica que rigió cada día de M1-M4 (guía maestra §9). Todos los valores numéricos de §7.3 fueron verificados por ejecución real (Node.js) antes de fijarse aquí.

## 8. Bibliografía — verificada por WebFetch/WebSearch real (2026-07-21)

| Fuente | Verificado | Relevancia directa para N9 |
|---|---|---|
| **Designing Data-Intensive Applications**, 2nd ed. (Kleppmann & Riccomini) | Estructura de capítulos confirmada: Parte I Fundamentos (modelos de datos, almacenamiento, codificación), Parte II Datos Distribuidos (Cap. 5 Replicación, Cap. 6 Particionamiento, Cap. 8 Problemas de sistemas distribuidos, Cap. 9 Consistencia y consenso), Parte III Datos Derivados | M3 completo (Cap. 5-6 son literalmente el contenido de Lab2-3); M1/M4 (Cap. 8-9, fallos parciales, qué significa "sano" en un sistema distribuido) |
| **MLOps Zoomcamp** (DataTalksClub, [GitHub](https://github.com/DataTalksClub/mlops-zoomcamp)) | Gratuito y autogestionado confirmado ("self-paced study", sin cohorte en vivo 2026); 6 módulos verificados: Introducción, Experiment Tracking, **Orquestación**, **Despliegue**, **Monitoreo (Prometheus/Evidently/Grafana)**, Buenas Prácticas (testing/CI-CD/IaC) | Módulo 3 (orquestación) → M1; Módulo 4 (despliegue: web service/streaming/batch) → M2; **Módulo 5 (monitoreo con Prometheus+Grafana) es prácticamente un ancla 1:1 con M4** |
| **Kubernetes docs** ([kubernetes.io/docs/concepts](https://kubernetes.io/docs/concepts/)) | Taxonomía completa verificada: Overview, Cluster Architecture, Workloads, Services/Networking, Storage, Configuration, Scheduling | Recorre en el mismo orden que M1.Lab1-3 |
| **vLLM docs** ([docs.vllm.ai](https://docs.vllm.ai/en/latest/getting_started/quickstart.html)) | Quickstart verificado: instalación, inferencia offline, servidor compatible OpenAI (`vllm serve <modelo>`) — confirmado que el quickstart NO cubre medición de rendimiento | M2.Lab1 |
| **vLLM `benchmarks/benchmark_serving.py`** ([GitHub oficial](https://github.com/vllm-project/vllm/blob/main/benchmarks/benchmark_serving.py)) | Confirmado como la herramienta oficial del propio proyecto para medir throughput/latencia bajo carga — no un script de terceros | M2.Lab2, ancla directa de la exigencia de "mediciones propias" de DOC-10 |

**Fuentes adicionales verificadas para la decisión de entornos (no bibliografía oficial de DOC-10, sino respaldo técnico de §3 — mismo criterio de ampliación justificada que N3 usó con Seeing Theory):** InsiderLLM (guía WSL2 GPU 2026), Docker Docs (soporte GPU en Docker Desktop), Lune.dev (fallo de detección de GPU en K8s sobre WSL2), kind.sigs.k8s.io (clústeres multi-nodo).

**Bibliografía ampliada de M1 (2026-07-22, verificada por WebSearch real, citada por decisión del Director de elevar el estándar a nivel académico/profesional):** Verma et al., *"Large-scale cluster management at Google with Borg"* (EuroSys 2015) · Burns, Grant, Oppenheimer, Brewer, Wilkes, *"Borg, Omega, and Kubernetes"* (CACM 2016) · Linux man-pages, `pid_namespaces(7)` (man7.org) · `tini` (krallin/tini, GitHub) · Basiri et al., *"Principles of Chaos Engineering"* (Netflix, 2017, arXiv:1702.05843) · Wiggins, *The Twelve-Factor App*, Factor III (12factor.net) · CIS Kubernetes Benchmark (Center for Internet Security) · Google Open Source Blog, *"The End of an Era: Transitioning Away from Ingress NGINX"* (2026) — confirma el retiro oficial de `ingress-nginx` en marzo 2026 · Gateway API (gateway-api.sigs.k8s.io, sucesor oficial de Ingress) · Amdahl (1967) y Gunther, *Universal Scalability Law* (perfdynamics.com) · KEDA (keda.sh, CNCF) · Argo Rollouts (argoproj.github.io/rollouts).

**Bibliografía ampliada de M2 (2026-07-22, verificada por WebSearch real):** Kwon, Li, Zhuang, Sheng, Zheng, Yu, Gonzalez, Zhang, Stoica, *"Efficient Memory Management for Large Language Model Serving with PagedAttention"* (SOSP 2023) — el paper original de vLLM · Lin, Tang, Tang, Yang, Chen, Wang, Xiao, Dang, Gan, Han, *"AWQ: Activation-aware Weight Quantization"* (MLSys 2024) · vLLM Documentation (instalación, cuantización, benchmarks — incluida la confirmación en vivo de que `benchmarks/benchmark_serving.py` está deprecado a favor de `vllm bench serve`) · Ray Serve Documentation (Anyscale) — conceptos, autoescalado, integración con vLLM · Brendan Gregg, *Benchmarking Checklist* (principios de medición de rendimiento de un ingeniero de referencia de la industria) · Docker Docs — Networking (host.docker.internal, la pieza real que conecta M1 y M2).

**Bibliografía ampliada de M3 (2026-07-22, verificada por WebSearch real):** Designing Data-Intensive Applications — Cap. 5 (Replication), Cap. 6 (Partitioning), Cap. 11 (Stream Processing), Martin Kleppmann — ya citado como bibliografía oficial de DOC-10, hoy aplicado con verificación empírica capítulo por capítulo · Redpanda Documentation (oficial) — conceptos, Schema Registry, alta disponibilidad · ByteByteGo, *RabbitMQ vs Kafka vs Pulsar* · Confluent, *Kafka vs Pulsar* y *Schema Evolution and Compatibility* · Kubernetes Documentation — StatefulSets (identidad de red estable, la base del hallazgo real de DNS del Día 14).

**Bibliografía ampliada de M4 (2026-07-22, verificada por WebFetch/WebSearch real):** Beyer, Jones, Petoff, Murphy (eds.), *Site Reliability Engineering* (Google, 2016) — capítulos "Monitoring Distributed Systems", "Service Level Objectives" y "Practical Alerting", la fuente primaria del vocabulario SLI/SLO/error budget y de los "golden signals" usados en Días 5, 6 y 14 · Google, *The SRE Workbook* — "Implementing SLOs" y "Alerting on SLOs" (sre.google/workbook), guía práctica citada en los Días 5 y 8 · Prometheus Documentation (oficial) — Instrumenting, Histograms and Summaries, Alerting Rules (prometheus.io/docs) · Grafana Documentation (oficial) — HTTP API Data Source, Dashboard JSON Model, ancla directa del hallazgo real de UID-vs-ID del Día 1 · vLLM Documentation — Production Metrics (docs.vllm.ai), métricas nativas activadas en el Día 3 · Austin Parker, Daniel Spoonhower et al., *Distributed Tracing in Practice* (O'Reilly) — marco conceptual de correlación por trace_id del Día 7 · David J. Agans, *Debugging: The 9 Indispensable Rules* — método sistemático aplicado al diagnóstico de arquitectura ajena del Día 15 · FinOps Foundation, *FinOps Framework* (Linux Foundation, finops.org) · AWS EC2 On-Demand Pricing — GPU Instances (aws.amazon.com, precios reales g5.xlarge/g6.xlarge verificados 2026) · Sarah Wang, Martin Casado, *"The Cost of Cloud, a Trillion Dollar Paradox"* (Andreessen Horowitz, 2021) — contexto real para la decisión local-vs-nube del Día 9 · Neil Gunther, *The Universal Scalability Law — A Guerrilla Guide* (perfdynamics.com) — ya citado en la bibliografía ampliada de M1, hoy reajustado con datos reales de contención de GPU (Día 11).

## 9. Pendiente

Los 4 módulos (M1-M4, 66 días, 138 ejercicios), el Capstone ET9 (6 hitos), y la compuerta (§7 — instrumentos, banco de examen escrito, banco de arquitecturas, banco práctico) están construidos y verificados íntegramente. Queda pendiente: auditoría desde el nivel siguiente y Herencias Declaradas hacia SYL-N10 (Pasos 6-7 — nota: N7/N8 pueden no estar congelados aún por construcción en paralelo, guía maestra §13.7), auditoría adversarial (Paso 8, idealmente múltiples auditores independientes sin coordinación entre sí, como el patrón de N1/N2/N3), Informe Final de Nivel (`docs/informes/n9-informe-final-de-nivel.md`), y la eventual declaración de candidato a v1.0.0 (Paso 9, autoridad exclusiva del Director).
